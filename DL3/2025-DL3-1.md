# 1. **Padding, Stride와 Layer size**

Convolutional Layer는 커널을 이용하여 이미지에서 feature를 추출하는 Layer입니다.

이미지는 Convolutional Layer를 통과할 때 padding을 따로 추가하지 않았다면 **사이즈가 점점 줄어듭니다.** 따라서 이를 방지하고자 이미지의 테두리에 padding을 추가하게 됩니다.

그 외에도 Convolutional Layer에서 조절할 수 있는 hyperparameter로는 **커널의 개수, 커널의 크기, stride** 등이 있습니다. 이들 모두 결과 이미지의 크기와 형태에 영향을 미치기 때문에 이들을 설정했을 때 결과 feature map이 어떻게 변할지 아는 것이 모델 구성의 첫걸음입니다.

이번 실습에서는 다양한 형태의 Convoltuional Layer를 Tensorflow를 통해 구현해보도록 하겠습니다.

## 지시사항

1. ```
   layers.Conv2D
   ```

    

   하나로 이루어진 모델을 만드는

    

   ```
   build_model1
   ```

    

   함수를 완성하세요. Layer 설정은 아래와 같습니다.

   - 커널 개수: `1개`
   - 커널 크기: `(3, 3)`
   - strides: `(1, 1)`
   - padding: `"same"`

1. ```
   layers.Conv2D
   ```

    

   두개로 이루어진 모델을 만드는

    

   ```
   build_model2
   ```

    

   함수를 완성하세요. 두 Layer 모두 아래와 같이 설정하세요.

   - 커널 개수: `4개`
   - 커널 크기: `(3, 3)`
   - strides: `(1, 1)`
   - padding: `"same"`

1. ```
   layers.Conv2D
   ```

    

   세개로 이루어진 모델을 만드는

    

   ```
   build_model3
   ```

    

   함수를 완성하세요. Layer 모두에 적용할 설정과 각 Layer 별 설정은 아래와 같습니다.

   - 공통 사항
     - 커널 크기: `(3, 3)`
     - strides: `(1, 1)`
   - 첫번째 Layer
     - 커널 개수: `2개`
     - padding: `"same"`
   - 두번째 Layer
     - 커널 개수: `4개`
     - padding: `"same"`
   - 세번째 Layer
     - 커널 개수: `8개`
     - padding은 따로 추가하지 않습니다.

모델이 잘 완성되었는지 확인하기 위해 가상의 이미지 텐서를 하나 만들어 모델을 통과한 크기가 어떻게 될지 확인할 수 있도록 코드가 완성되어 있습니다. 모델을 모두 구현 후 실행을 통해 확인해보세요.



# 2. MLP로 이미지 데이터 학습하기

이번 실습에서는 Fully-connected Layer를 쌓아 만든 Multilayer Perceptron(MLP) 모델을 사용하여 이미지 데이터를 학습해보도록 하겠습니다.

앞서 이미지 데이터 과목에서 개와 고양이 데이터셋을 통해 실습을 진행했었습니다. 이번 실습부터는 실제 논문에서도 모델 검증을 위해 자주 사용하는 데이터셋인 **CIFAR-10** 데이터셋을 사용할 것입니다.

![img](https://cdn-api.elice.io/api-attachment/attachment/ee017699b95d478eb713750caf4fd6e2/cifar-10.png)

CIFAR-10 데이터셋은 각 데이터가 32 ×× 32의 크기를 가지는 컬러 이미지로 구성되어 있습니다. 학습(Train) 데이터셋은 50000개, 테스트(Test) 데이터셋은 10000개의 이미지가 포함되어 있습니다. 각 이미지는 위의 그림에서처럼 10개의 클래스 중 하나에 속합니다.

본 실습에서는 학습 데이터로 50000개와 테스트 데이터로 10000개를 모두 사용하기에는 제약이 있어 각각 7500개와 500개만 가져와 모델을 학습시키고 테스트해볼 것입니다.

## 지시사항

이번 실습에서는 CIFAR-10의 학습 데이터셋에서 7500개의 이미지와 테스트 데이터셋에서 500개의 이미지를 따로 뽑아내어 numpy array 파일(`.npy` 파일)로 구성하여 `dataset` 폴더 내에 저장되어 있습니다.

따라서 데이터를 불러오기 위해 별도로 `ImageDataGenerator`를 쓰지 않고, `load_cifar10_dastaset` 함수에서 바로 이 numpy array를 불러오도록 구현되어 있습니다.

numpy array는 그 자체로 바로 tensorflow의 `fit` 함수에 사용할 수 있습니다. 이 점 참고하여 코드를 완성하세요.

1. MLP 모델을 구성하는 함수

    

   ```
   build_mlp_model
   ```

   을 완성하세요.

   - MLP 모델이므로 2차원 이미지를 1차원으로 변형해야 합니다. 맨 먼저 `layers.Flatten` Layer를 추가하세요.

   - 이후 마지막을 제외한 각

      

     ```
     layers.Dense
     ```

      

     Layer의 노드 개수는 아래와 같습니다. 모두 활성화 함수는

      

     ReLU

     로 설정하세요.

     - `4096`개
     - `1024`개
     - `256`개
     - `64`개

   - 마지막 Layer는 활성화 함수가 **Softmax**이고 노드 개수는 `num_classes`개인 `layers.Dense` Layer입니다.

1. `main` 함수에서 `optimizer`는 **Adam**으로 설정하세요. Learning rate은 `0.001`로 설정하세요.

1. 모델의 손실 함수와 평가 지표(metrics)를 아래와 같이 설정하세요. optimizer는 2번에서 불러온 것을 사용합니다.
   - 손실 함수: `sparse_categorical_crossentropy`
   - 평가 지표(metrics): `"accuracy"`

1. 모델 학습을 위한 hyperparameter는 아래와 같이 설정하세요.
   - `epochs=epochs`
   - `batch_size=64`
   - `validation_split=0.2`
   - `shuffle=True`
   - `verbose=2`



# 3. MLP와 CNN 모델 비교

이번 실습에서는 앞선 실습에서 CNN 모델을 추가하여 둘의 성능을 비교해보도록 하겠습니다. 데이터셋은 동일한 CIFAR-10 데이터셋을 사용하도록 하겠습니다.

이번 실습에서는 두 모델의 성능을 비교하기 위해 모델이 가지는 파라미터의 개수를 확인하는 코드가 추가되어 있습니다. Tensorflow에서는 아래와 같이 모델에 `summary` 함수를 실행하면 모델이 가지는 파라미터 개수를 확인할 수 있습니다.

```
cnn_model.summary()
Copy
```

### 출력 예시

![img](https://cdn-api.elice.io/api-attachment/attachment/a20f49c101bf447391b8ad52171af11e/model_summary.png)

여기서 가장 하단에 나오는 `Trainable params`의 개수가 실제 모델 학습에 사용되는 파라미터의 개수입니다. MLP와 CNN 모델이 각각 이 값이 어떻게 나오는지 확인해보고, 최종 학습 성능과 테스트 성능이 어떻게 나오는지도 비교해보세요.

## 지시사항

1. MLP 모델을 구성하는 함수

    

   ```
   build_mlp_model
   ```

   을 완성하세요. 앞선 실습과 동일한 값으로 설정하면 됩니다.

   - MLP 모델이므로 2차원 이미지를 1차원으로 변형해야 합니다. 맨 먼저 `Flatten` Layer를 추가하세요.

   - 이후 마지막을 제외한 각

      

     ```
     Dense
     ```

      

     Layer의 노드 개수는 아래와 같습니다. 모두 활성화 함수는

      

     ReLU

     로 설정하세요.

     - `4096`개
     - `1024`개
     - `256`개
     - `64`개

   - 마지막 Layer는 활성화 함수가 **Softmax**이고 노드 개수는 `num_classes` 개인 `Dense` Layer입니다.

1. CNN 모델을 구성하는 함수

    

   ```
   build_cnn_model
   ```

   을 완성하세요. 모델 Layer 구성은 아래와 같습니다. 모든

    

   ```
   Conv2D
   ```

    

   Layer의 활성화 함수는 ReLU로 설정하세요.

   - ```
     layers.Conv2D
     ```

     - 커널 개수: `16개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - `input_shape=img_shape`

   - ```
     layers.Conv2D
     ```

     - 커널 개수: `32개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`

   - ```
     layers.MaxPool2D
     ```

     - 이미지 사이즈가 2배로 줄도록 설정하세요.

   - ```
     layers.Conv2D
     ```

     - 커널 개수: `64개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - strides: `(2, 2)`

   - ```
     layers.Conv2D
     ```

     - 커널 개수: `64개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - strides: `(2, 2)`

   - ```
     layers.MaxPool2D
     ```

     - 이미지 사이즈가 2배로 줄도록 설정하세요.

   - `layers.Flatten`

   - ```
     layers.Dense
     ```

     - 노드 개수: `128개`
     - 활성화 함수: **ReLU**

   - ```
     layers.Dense
     ```

     - 노드 개수: `num_classes`
     - 활성화 함수: **Softmax**

1. `run_model` 함수에서 `optimizer`는 **Adam**으로 설정하세요. Learning rate은 `0.001`로 설정하세요.

1. ```
   run_model
   ```

    

   함수에서 모델의 손실 함수와 평가 지표(metrics)를 아래와 같이 설정하세요. optimizer는 2번에서 불러온 것을 사용합니다.

   - 손실 함수: `sparse_categorical_crossentropy`
   - 평가 지표(metrics): `"accuracy"`

1. ```
   run_model
   ```

    

   함수에서 모델 학습을 위한 hyperparameter를 아래와 같이 설정하세요.

   - `epochs=epochs`
   - `batch_size=64`
   - `validation_split=0.2`
   - `shuffle=True`
   - `verbose=2` 



# 4. **VGG16 모델 구현하기**

VGGNet은 2014년 ImageNet Challenged에서 2등을 차지한 모델입니다. VGGNet의 의의는 모든 커널의 크기를 3 ×× 3으로 고정하여 본격적으로 CNN 모델의 Layer 개수를 늘리기 시작했다는 것에 있습니다.

VGGNet은 기존 AlexNet의 Layer 개수보다 두배 이상 늘어난 16개와 19개의 두가지 모델이 있습니다. 이번 실습에서는 이 중에서 16개로 이루어진 VGGNet, 즉 VGG16 모델을 구현하도록 하겠습니다.

## 지시사항

VGGNet부터는 Layer 개수가 많이 늘어남에 따라 **Block 단위**로 모델을 구성하게 됩니다. 각 Block은 2개 혹은 3개의 Convolutional Layer와 Max Pooling Layer로 구성되어 있습니다.

지시사항에 따라 `build_vgg16` 함수 내의 각 Block들을 완성하세요.다만 채점은 앞선 실습과 달리 지시사항 별로 확인하는 것이 아닌 **전체 모델의 Layer가 올바르게 구성되었는지를 보고** 이루어지게 됩니다.

1. 첫번째 Block을 완성하세요. Layer 구성은 아래와 같습니다.

   - 두개

     의

      

     ```
     layers.Conv2D
     ```

     - 커널 개수: `64개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`

   - `layers.MaxPooling2D`: 이미지 사이즈가 2배로 줄어들도록 하세요.

1. 두번째 Block을 완성하세요. Layer 구성은 아래와 같습니다.

   - 두개

     의

      

     ```
     layers.Conv2D
     ```

     - 커널 개수: `128개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`

   - `layers.MaxPooling2D`: 이미지 사이즈가 2배로 줄어들도록 하세요.

1. 세번째 Block을 완성하세요. Layer 구성은 아래와 같습니다.

   - 세개

     의

      

     ```
     layers.Conv2D
     ```

     - 커널 개수: `256개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`

   - `layers.MaxPooling2D`: 이미지 사이즈가 2배로 줄어들도록 하세요.

1. 네번째 Block을 완성하세요. Layer 구성은 아래와 같습니다.

   - 세개

     의

      

     ```
     layers.Conv2D
     ```

     - 커널 개수: `512개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`

   - `layers.MaxPooling2D`: 이미지 사이즈가 2배로 줄어들도록 하세요.

1. 다섯번째 Block을 완성하세요. Layer 구성은 **네번째 Block과 완전히 동일합니다.**

모델을 모두 구현하면 모델의 Layer를 확인하기 위한 `summary` 함수를 실행합니다. 출력 결과를 보고 지시사항의 Layer와 동일한지 확인해보세요.



# 5. **ResNet 구현하기**

ResNet에 처음 소개된 Residual Connection은 모델 내의 지름길을 새로 만든다고도 하여 Skip Connection이라고도 불리며, 레이어 개수가 매우 많은 경우에 발생할 수 있는 **기울기 소실(Vanishing Gradient)** 문제를 해결하고자 등장하였습니다.

![image](https://cdn-api.elice.io/api-attachment/attachment/e5c43920e91946aca57c07b304146057/Residual%20Block.JPG)

이번 실습에서는 위 그림과 유사한 Residual Connection을 구현해보도록 하겠습니다.

## 지시사항

Residual Connection은 보통 ResNet의 각 Block 단위로 들어가 있습니다. 따라서 일반적으로 Residual Connection을 가지는 부분을 Residual Block이라 하여 Block 단위로 구현한 후에 이들을 연결하는 식으로 모듈화 하여 전체 모델을 구현하게 됩니다.

Residual Block은 `ResidualBlock` 이란 이름의 클래스로 현재 스켈레톤이 구현되어 있습니다. 지시사항에 맞춰 클래스 내부를 채워 Residual Block을 완성하세요.

1. ```
   ResidualBlock
   ```

    

   클래스를 완성하세요.

   - 두개

     의

      

     ```
     layers.Conv2D
     ```

      

     Layer

     - 커널 개수: `num_kernels`
     - 커널 크기: `kernel_size`
     - padding: `"same"`
     - 활성화 함수: `ReLU`

   - 두 텐서를 더하는 `layers.Add` Layer

Residual Block을 완성했다면 이를 활용하는 간단한 ResNet 모델이 `build_resnet` 함수를 통해 만들어집니다. `model.summary()` 결과를 통해 Residual Block이 어떻게 모델에 들어가게 되는지 확인해보세요.



# 6. **CNN 모델로 Fashion MNIST 분류하기**

이번 실습에서는 단순한 CNN 모델을 통해 Fashion MNIST 데이터셋을 분류하도록 하겠습니다.

![img](https://cdn-api.elice.io/api-attachment/attachment/350ea97abc1543f4addbaf8257953da2/fashion_mnist-3.0.1.png)

이론 자료에서 사용했던 모델에 Max Pooling layer와 Global Average Pooling layer를 더해서 모델을 구성하도록 하겠습니다.

여기서 Global Average Pooling layer는 Flatten layer의 역할을 대신합니다. 이 layer는 이름에서 알 수 있듯이 average pooling을 수행하는 역할을 하나, 기존 pooling 연산과 달리 전체 이미지 영역에서 수행한다는 차이점이 있습니다.

![img](https://cdn-api.elice.io/api-attachment/attachment/974cacb74968454594c23ad432e38869/global_average_pooling.png)

따라서 feature map의 각 feature 마다 하나의 값을 뽑아내게 되어 2차원이었던 이미지 데이터를 1차원으로 줄이는 효과를 가지고 있습니다. 이렇게 하면 단순히 Flatten layer를 사용하는 것보다 파라미터 수를 더 줄일 수 있기 때문에 최근 CNN 모델에서는 Global Average Pooling layer를 많이 도입하고 있습니다.

## 지시사항

아래 지시사항에 따라 코드를 완성하세요.

1. Fashion MNIST 데이터셋을 불러오는 함수 `load_fashion_mnist`를 완성하세요.
   - 이미지의 각 픽셀값을 0과 1 사이로 정규화하세요. 원본 이미지의 픽셀값은 0과 255 사이의 정수값입니다.
   - 입력 데이터(`train_X`, `test_X`)의 차원을 맨 뒷쪽으로 하나 늘리세요. 즉, 현재 각 이미지의 shape을 `(28, 28)`에서 `(28, 28, 1)` 변경하세요.
   - 정수 형태로 이루어진 라벨 데이터를 one-hot encoding으로 바꾸세요. Fashion MNIST 데이터셋의 클래스 개수는 10개입니다.

1. CNN 모델을 만드는 함수 `build_cnn_model` 함수를 완성하세요. 모델의 layer 구성은 다음과 같습니다.
   - `Input` layer: 입력 텐서의 shape 지정
   - `Conv2D` layer **2개**: 필터 **32개**, 커널 크기 **(3, 3)**, padding은 **same**, 활성화 함수 **ReLU**
   - `MaxPool2D` layer
   - `Conv2D` layer **2개**: 필터 **64개**, 커널 크기 **(3, 3)**, padding은 **same**, 활성화 함수 **ReLU**
   - `MaxPool2D` layer
   - `Conv2D` layer **2개**: 필터 **128개**, 커널 크기 **(3, 3)**, padding은 **same**, 활성화 함수 **ReLU**
   - `GlobalAveragePooling2D`
   - `Dense`: 노드 개수 `num_classes` 개, 활성화 함수 **Softmax**

1. Epoch 별로 train loss와 valid loss를 그리는 함수 `plot_loss`를 완성하세요.
   - `History` 객체에서 train loss와 valid loss를 불러오세요
   - matplotlib의 `ax`를 통해 train loss와 valid loss를 plot 하세요.

1. Epoch 별로 train accuracy와 valid accuracy를 그리는 함수 `plot_accuracy`를 완성하세요.
   - `History` 객체에서 train accuracy와 valid accuracy를 불러오세요
   - matplotlib의 `ax`를 통해 train accuracy와 valid accuracy를 plot 하세요.

1. 모델의 optimizer, loss, metrics은 다음과 같이 설정하세요.
   - Optimizer: **Adam** (learning rate: 0.001)
   - Loss: **Cross Entropy** (one-hot encoding에 알맞는 Cross Entropy를 사용해야 합니다.)
   - Metrics: **Accuracy**

1. 모델을 학습하고 테스트 하는 코드를 완성하세요. 학습 hyperparameter는 다음과 같이 설정하세요.
   - `epochs=epochs`
   - `batch_size`: 256
   - `validation_split`: 0.2
   - `shuffle`: True

1. 학습한 모델의 Test loss와 Test accuracy를 구하세요.
   - 전체 학습 및 테스트에 **10분** 정도 소요됩니다.



# 7. **CNN 모델로 CIFAR-10 분류하기**

이번 실습에서는 CNN 모델을 통해 CIFAR-10 이미지 데이터셋을 분류하도록 하겠습니다. CIFAR-10 데이터셋은 MNIST와 달리 컬러 이미지로 이루어진 데이터셋이며, 각 이미지의 크기는 32 ×× 32 입니다.

![img](https://cdn-api.elice.io/api-attachment/attachment/95377f73a1d24b2da9944f26b39b93ef/cifar10.png)

이번에는 그동안 단순 정확도만 구하던 것에 더해 한가지 평가 지표를 더 구할 것입니다. 바로 **Top-K Accuracy**라 불리는 지표입니다. 기존 정확도는 예측값이 실제값과 정확히 일치하는 것만의 비율을 보여주었습니다.

하지만 딥러닝 모델의 예측값은 **확률값**으로 나오기 때문에 **확률이 높게 나온 K개**를 뽑아 **이 중에 실제값이 포함되어 있는지**를 확인할 수도 있습니다. 여기서 K개 안에 실제 값이 있다면 맞춘 것으로 간주하고 구한 정확도가 바로 Top-K Accuracy입니다.

아래 그림에서 위의 경우는 정답이 5인 경우라서 5일 확률이 제일 높을 때만 정답 처리가 됩니다. 하지만 아래의 경우는 정답이 2일 확률이 세번째로 높지만 Top-3 Accuracy를 구하는 상황이므로 이 또한 정답 처리를 하게 됩니다.

![img](https://cdn-api.elice.io/api-attachment/attachment/59a64568ea7848af9939628f962379c0/topk_accuracy.png)

이러한 Top-K Accuracy는 일반적으로 클래스가 많은 데이터셋에 분류 작업을 수행할 때 많이 살펴보게 됩니다. 가장 대표적인 예로 ImageNet의 경우 클래스가 1000개나 되기 때문에 Top-1 Accuracy로는 성능 변화가 크게 보이지 않을 수 있습니다. 이런 경우에 Top-5 Accuracy 등도 같이 보게 된다면 **성능의 변화를 좀더 극적으로 확인할 수 있게** 됩니다.

## 지시사항

아래 지시사항에 따라 코드를 작성하세요. 최종 **테스트 정확도(Top-1 Accuracy)는 55%** 이상, **Top-3 Accuracy는 85%** 이상 나와야 합니다. **실행 제한 시간은 30분입니다.**

1. CIFAR-10 데이터셋을 불러오는 함수 `load_cifar10`를 완성하세요.
   - 이미지의 각 픽셀값을 0과 1 사이로 정규화하세요.
   - 정수 형태로 이루어진 라벨 데이터를 one-hot encoding으로 바꾸세요. CIFAR-10 데이터셋의 클래스 개수는 10개입니다.

1. CNN 모델을 만드는 함수 `build_cnn_model` 함수를 완성하세요. 모델의 layer 구성은 다음과 같습니다. 모든 `layers.Conv2D`의 활성화 함수는 **ReLU**로 지정하세요.
   - `Input`
     - `shape=input_shape`으로 입력 데이터의 모양을 지정해줍니다.
   - `layers.Conv2D`
     - 커널 개수: `16개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
   - `layers.Conv2D`
     - 커널 개수: `32개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
   - `layers.MaxPool2D`
     - 이미지 사이즈를 **2배**로 줄이세요.
   - `layers.Conv2D`
     - 커널 개수: `64개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
   - `layers.Conv2D`
     - 커널 개수: `128개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
   - `layers.MaxPool2D`
     - 이미지 사이즈를 **2배**로 줄이세요.
   - `layers.Conv2D`
     - 커널 개수: `256개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
   - `layers.Conv2D`
     - 커널 개수: `512개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
   - `layers.MaxPool2D`
     - 이미지 사이즈를 **2배**로 줄이세요.
   - `layers.GlobalAveragePooling2D`
   - `layers.Dense`
     - 노드 개수: `100개`
     - 활성화 함수: `ReLU`
   - `layers.Dropout`
     - rate : `0.5`
     - Dropout 에 대해서는 3장에서 자세하게 배웁니다.
   - `layers.Dense`
     - 노드 개수: `num_classes 개`
     - 활성화 함수: `Softmax`

1. Epoch 별로 train loss와 valid loss를 그리는 함수 `plot_loss`를 완성하세요.
   - `History` 객체에서 train loss와 valid loss를 불러오세요
   - matplotlib의 `ax`를 통해 train loss와 valid loss를 plot 하세요.

1. Epoch 별로 train accuracy와 valid accuracy를 그리는 함수 `plot_accuracy`를 완성하세요.
   - `History` 객체에서 train accuracy와 valid accuracy를 불러오세요
   - matplotlib의 `ax`를 통해 train accuracy와 valid accuracy를 plot 하세요.

1. Top-k Accuracy를 구하는 함수 `get_topk_accuracy`를 완성하세요.
   - 실제 라벨을 one-hot encoding으로 가지고 있는 `test_y`를 다시 정수 라벨 형식으로 바꾸세요.
   - `pred_y`에 있는 확률값을 작은 값에서 큰 값으로 정렬하세요.
   - `for`문 내에서 현재 `pred_label`에 있는 확률값 중 가장 큰 k개를 가져오세요.
   - 마지막으로 Top-k Accuracy를 구하세요.

1. 모델의 optimizer, loss, metrics은 다음과 같이 설정하세요.
   - Optimizer: **SGD** (learning rate: 0.01, momentum: 0.9, nesterov: True)
   - Loss: **Cross Entropy** (one-hot encoding에 알맞는 Cross Entropy를 사용해야 합니다.)
   - Metrics: **Accuracy**

1. 모델을 학습하고 테스트 하는 코드를 완성하세요. 학습 hyperparameter는 다음과 같이 설정하세요.
   - `epochs=epochs`
   - `batch_size`: 32
   - `validation_split`: 0.2
   - `shuffle`: True

1. 학습한 모델에 테스트 데이터를 적용하여 예측값을 구하는 코드를 완성하세요. 이후 학습한 모델의 Test accuracy를 구하세요.



# 8. **LeNet-5 모델 만들기**

이번 실습에서는 LeNet-5 모델을 직접 구현해보고 Fashion MINST 데이터셋으로 학습해보도록 하겠습니다.

![img](https://kjhov195.github.io/post_img/200210/image0.png)

## 지시사항

지시사항을 모두 작성하고 나면 **실행과 채점에 다소 시간이 소요될 수 있습니다.**

1. Fashion MNIST 데이터셋을 불러오는 함수 `load_fashion_mnist`를 완성하세요.
   - 이미지의 각 픽셀값을 0과 1 사이로 정규화하세요. 원본 이미지의 픽셀값은 0과 255 사이의 정수값입니다.
   - 입력 데이터(`train_X`, `test_X`)의 차원을 맨 뒷쪽으로 하나 늘리세요. 즉, 현재 각 이미지의 shape을 `(28, 28)`에서 `(28, 28, 1)` 변경하세요.
   - 정수 형태로 이루어진 라벨 데이터를 one-hot encoding으로 바꾸세요. Fashion MNIST 데이터셋의 클래스 개수는 10개입니다.

1. LeNet 모델을 만드는 함수 `build_lenet_model`을 완성하세요. 모델 Layer 구성은 다음과 같습니다.
   - `layers.Conv2D`
     - 커널 개수: `6개`
     - 커널 크기: `(5, 5)`
     - strides: `(1, 1)`
     - padding: `"same"`
     - 활성화 함수: `tanh`
   - `layers.AveragePooling2D`
     - pool_size: `(2, 2)`
     - strides: `(2, 2)`
     - padding: `"valid"`
   - `layers.Conv2D`
     - 커널 개수: `16개`
     - 커널 크기: `(5, 5)`
     - strides: `(1, 1)`
     - padding: `"valid"`
     - 활성화 함수: `tanh`
   - `layers.AveragePooling2D`
     - pool_size: `(2, 2)`
     - strides: `(2, 2)`
     - padding: `"valid"`
   - `layers.Flatten`
   - `layers.Dense`:
     - 노드 개수: `120개`
     - 활성화 함수: `tanh`
   - `layers.Dense`
     - 노드 개수: `84개`
     - 활성화 함수: `tanh`
   - `layers.Dense`
     - 노드 개수: `num_classes 개`
     - 활성화 함수: `Softmax`

1. 모델 학습을 위한 optimizer, 손실 함수, 평가 지표(Metrics)를 설정하세요.
   - Optimizer: `Adam`
   - Learning Rate: `0.001`
   - Loss 함수: `categorical_crossentropy`
   - Metrics: `Accuracy`

1. 모델을 학습하기 위한 hyperparameter는 다음과 같이 설정하세요.
   - `epochs=epochs`
   - `batch_size`: 128
   - `shuffle`: True
   - `verbose`: 2

1. 학습한 모델에 테스트 데이터를 적용하여 Test 정확도를 구하세요.



# 9. **VGG-11 모델 만들기**

이번 실습에서는 VGG-11 모델을 직접 구현해보고 Fashion MINST 데이터셋으로 학습해보도록 하겠습니다.

![img](https://neurohive.io/wp-content/uploads/2018/11/vgg16-1-e1542731207177.png)

VGG-11 모델은 [원 논문](https://arxiv.org/abs/1409.1556)에서 실험한 여러 VGG 모델 중 가장 Layer 수가 적은 모델입니다 (아래 그림에서 A 모델)

![img](https://cdn-api.elice.io/api-attachment/attachment/ad323c16f5bb4e2688474c82c679f331/vgg11.png)

이론에서는 VGG-16과 VGG-19를 주로 배웠으나, 이들을 실습 환경에서 직접 사용하기엔 너무 오랜 시간이 걸리기 때문에 이 VGG-11 모델을 사용하도록 하겠습니다.

또한 원 논문에서 제시한 커널 개수 또한 Fashion MNIST를 학습하기에는 너무 많은 파라미터 수를 만드는 것이기 때문에 이 또한 줄여서 학습할 것입니다.

## 지시사항

앞서 언급한 대로 이번 실습에서 사용할 VGG-11 모델은 Convolutional Layer의 커널 개수가 원 논문에서 제시한 개수보다 적습니다.

하지만 그럼에도 VGG 모델 자체가 많은 파라미터 수를 가지기 때문에 학습에 오랜 시간이 걸립니다.

**기본으로 사용하는 epochs 수는 2**입니다. 코드를 완성하여 실행하게 되면 최대 5분 정도의 시간이 소요될 것입니다.

지시사항에 따라 코드를 완성하세요.

1. Fashion MNIST 데이터셋을 불러오는 함수 `load_fashion_mnist`를 완성하세요.
   - 이미지의 각 픽셀값을 0과 1 사이로 정규화하세요. 원본 이미지의 픽셀값은 0과 255 사이의 정수값입니다.
   - 입력 데이터(`train_X`, `test_X`)의 차원을 맨 뒷쪽으로 하나 늘리세요. 즉, 현재 각 이미지의 shape을 `(28, 28)`에서 `(28, 28, 1)` 변경하세요.
   - 정수 형태로 이루어진 라벨 데이터를 one-hot encoding으로 바꾸세요. Fashion MNIST 데이터셋의 클래스 개수는 10개입니다.

1. VGG-11 모델을 만드는 함수 `build_vgg_model`을 완성하세요. 모델 Layer 구성은 다음과 같습니다.
   - `layers.Conv2D`
     - 커널 개수: `16개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.MaxPool2D`
     - pool_size: `(2, 2)`
     - strides: `(2, 2)`
   - `layers.Conv2D`
     - 커널 개수: `32개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.MaxPool2D`
     - pool_size: `(2, 2)`
     - strides: `(2, 2)`
   - `layers.Conv2D`
     - 커널 개수: `64개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.Conv2D`
     - 커널 개수: `64개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.Conv2D`
     - 커널 개수: `128개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.Conv2D`
     - 커널 개수: `128개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.Conv2D`
     - 커널 개수: `128개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.Conv2D`
     - 커널 개수: `128개`
     - 커널 크기: `(3, 3)`
     - padding: `"same"`
     - 활성화 함수: `ReLU`
   - `layers.MaxPool2D`
     - pool_size: `(2, 2)`
     - strides: `(2, 2)`
   - `layers.Flatten`
   - `layers.Dense`:
     - 노드 개수: `32개`
     - 활성화 함수: `ReLU`
   - `layers.Dense`
     - 노드 개수: `32개`
     - 활성화 함수: `ReLU`
   - `layers.Dense`
     - 노드 개수: `num_classes 개`
     - 활성화 함수: `Softmax`

1. 모델 학습을 위한 optimizer, 손실 함수, 평가 지표(Metrics)를 설정하세요.
   - Optimizer: `Adam`
   - Learning Rate: `0.001`
   - Loss 함수: `categorical_crossentropy`
   - Metrics: `Accuracy`

1. 모델을 학습하기 위한 hyperparameter는 다음과 같이 설정하세요.
   - `epochs=epochs`
   - `batch_size`: 64
   - `validation_split`: 0.2
   - `shuffle`: True
   - `verbose`: 1

1. 학습한 모델에 테스트 데이터를 적용하여 Test 정확도를 구하세요.

## 참고

코드를 올바르게 작성하여 실행을 하게 되면 배치 별로 학습 과정을 보여주는 **진행바(progress bar)** 가 한줄씩 출력될 것입니다.

![img](https://cdn-api.elice.io/api-attachment/attachment/4374c7a5c1d64f73b40b28903d4492a8/progress_bar.png)